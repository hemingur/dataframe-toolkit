# dftk.common — shared utilities for the dftk toolkit.
#
# Modules:
#   io.py   — dataframe I/O, parquet-pipe transport, output transformations
#
# Future candidates for this package:
#   seed.py — random seed normalisation (int or MD5-hashed string → int)
#   ci.py   — confidence interval helpers shared across stat/pivot/wstat
